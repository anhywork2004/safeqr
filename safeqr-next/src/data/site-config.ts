// ============================================
// SafeQR v2 — Static Site Config Fallback
// ============================================
import { DEFAULT_CONFIG } from "@/types";

export { DEFAULT_CONFIG };
